# 更新日志

## 2.3.0 - 2026-07-19

### 低功耗

- Windows 桌面界面关闭硬件加速，不再持续唤醒 NVIDIA 独立显卡。
- 窗口隐藏到系统托盘后暂停 DOM 更新和 Canvas 曲线重绘，重新打开时一次性读取完整历史。
- 去除大面积实时模糊与毛玻璃效果，降低软件渲染开销并保留原有视觉层次。
- 采集任务改为完成后再安排下一次采样，避免慢查询发生重叠。

### 新增

- 默认采样间隔调整为 10 秒，可在“使用设置”选择 5 秒、10 秒或 30 秒。
- 每条记录保存当时的采样间隔，切换频率后仍能正确判断掉线区间。
- 设置页显示 PulseBoard 自身 CPU 与内存占用。
- 顶部增加低功耗状态标识。

### 验证

- RTX 3090 测试机运行 2.3 时，NVIDIA 活动进程中不再出现 PulseBoard。
- 同一台测试机实测窗口显示约占 0.106% CPU，隐藏到托盘约占 0.057% CPU。
- 隐藏到托盘后仍按设定间隔持续写入历史记录。

## 2.2.0 - 2026-07-15

### 新增

- Windows 桌面版默认开启开机自启动，并以隐藏窗口方式在系统托盘开始采集。
- 增加“使用设置”窗口，可随时开启或关闭开机自启动。
- 增加连续记录时长、所选范围离线时长、有效数据点和启动状态概览。
- 增加 CSV 导出，包含在线/离线状态与完整资源数值。
- 增加单实例保护，避免重复启动产生重复采集记录。

### 修复

- 长时间没有采样时自动补充离线开始与结束点，所有资源值归零。
- 曲线横坐标改为真实时间比例，不再把关机前后的数据直接连接。
- 历史抽样保留离线标记及相邻在线点，长时间范围仍能准确显示掉线状态。
- Windows 与 NAS / Linux Web 版统一显示离线区间。

### 界面

- 新增灰色离线区域、边界与文字提示。
- 优化顶部状态、卡片层次、按钮反馈、数据概览和移动端布局。
- 横坐标增加中间时间刻度，工具提示明确标记设备离线状态。

## 2.1.0 - 2026-07-12

### 新增

- Windows 桌面版增加“手机访问”开关。
- 自动识别局域网 IP，并生成手机访问地址与二维码。
- 手机端每 5 秒同步桌面版实时资源数据。
- 手机端直接读取桌面版同一历史目录，支持 1 小时至 30 天回看。
- 增加独立访问密码、复制地址和 Windows 防火墙提示。

### 安全

- 手机共享默认关闭。
- 仅在用户开启时监听局域网端口，关闭后立即停止服务。
- 访问密码保存在本机应用设置目录。
- Web 登录使用 HttpOnly、SameSite=Strict 会话 Cookie。

## 2.0.0 - 2026-07-12

### 新增

- Linux / NAS Web 版，可通过电脑、手机和平板浏览器访问。
- Docker Compose 一键部署，支持 AMD64 与 ARM64。
- 直接读取 Linux 主机 `/proc` 与 `/sys` 的 CPU、内存、磁盘和网络指标。
- 内置密码登录与 7 天登录会话。
- 响应式移动端布局。
- NVIDIA Container Toolkit 可选 GPU 监控。
- GitHub Actions 自动构建并发布多架构 GHCR 镜像。

### 安全与数据

- 历史数据通过 `/data` 挂载目录持久保存。
- `/proc` 与 `/sys` 使用只读挂载。
- 容器启用 `no-new-privileges`，并提供健康检查。

## 1.1.0 - 2026-07-12

### 新增

- 所有历史曲线增加纵坐标刻度和单位。
- 鼠标悬停曲线时显示精确采样时间和对应数值。
- 点击曲线可以锁定十字线与数据提示，再次点击解除锁定。
- 多序列曲线分别显示每条序列的名称、颜色和数值。

### 优化

- 百分比曲线固定使用 0%–100% 刻度，便于横向比较。
- 磁盘与网络曲线根据实际峰值自动选择易读量程。
- 时间提示精确到秒，并在切换时间范围时自动重置选择状态。

## 1.0.0 - 2026-07-12

- 首次公开发布。
- 支持 CPU、内存、GPU、显存、磁盘和网络监控。
- 支持保存并回看最近 30 天历史数据。
